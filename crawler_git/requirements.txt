# Python 包依赖（pip install -r requirements.txt）
playwright>=1.40.0
requests>=2.31.0

# Playwright 浏览器（pip 安装后需单独执行，非 pip 包）
# playwright install chromium

# 系统/外部应用（macOS，需自行安装，不在 pip 范围内）
# - Downie 4：YouTube 视频下载（downie4/download_youtube.py）
# - PrivadoVPN：自动化工作流 VPN 步骤（vpn/open_privadovpn.py）
# - Python 3.7+
